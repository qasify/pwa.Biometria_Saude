import {
  INVALID_BIRTHDATE,
  INVALID_CPF,
  INVALID_EMAIL,
  INVALID_PHONE,
} from "./errors";

const constants = {
  INVALID_CPF,
  INVALID_EMAIL,
  INVALID_PHONE,
  INVALID_BIRTHDATE,
};

export default constants;
